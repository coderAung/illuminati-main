$("document").ready(() => {
	$("#search-user-btn").click(() => {
		document.getElementById("search-user-btn").querySelector("a").click()
	})

	$("#search-post-btn").click(() => {
		document.getElementById("search-post-btn").querySelector("a").click()
	})
})